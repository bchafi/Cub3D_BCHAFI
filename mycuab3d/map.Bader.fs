>> check the `ac` is not 2 / exit(1000).
> check_file():
    >> check final ".cub" (lenght < 3) / exist(10001).
    >> check the file is a directory / exit(1002).
    >> check the file is a FILE / exit(1003).
>> read_file():
    >> 